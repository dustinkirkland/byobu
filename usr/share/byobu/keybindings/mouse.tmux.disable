set -g mouse on
run-shell 'sed -i "/^set -g mouse /d" "$BYOBU_CONFIG_DIR/.tmux.conf" 2>/dev/null; printf "set -g mouse on\n" >> "$BYOBU_CONFIG_DIR/.tmux.conf"'
bind-key -n M-F12 source $BYOBU_PREFIX/share/byobu/keybindings/mouse.tmux.enable \; display-message "Mouse: OFF"
