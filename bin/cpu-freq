#!/bin/sh -e

mhz=`grep -m 1 "^cpu MHz" /proc/cpuinfo | sed "s/^.*: //" | sed "s/\..*$//"`
if [ $mhz -ge 1000 ]; then
	ghz=$(echo "$mhz" | awk '{ printf "%.1f", $1 / 1000 }')
	echo "$ghz""GHz"
else
	echo "$mhz""MHz"
fi
